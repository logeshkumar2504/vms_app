<Window x:Class="Vms_page.DxVideoWallDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Video Wall Info"
        Height="520" Width="960"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundColor}">
    <Window.Resources>
        <!-- Local copy of CompactComboBoxStyle used app-wide -->
        <Style x:Key="CompactComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleButton"
                                          Grid.Column="2"
                                          Focusable="false"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <Path x:Name="Arrow"
                                                  Fill="{DynamicResource TextSecondaryColor}"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,4,0"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Template="{DynamicResource ComboBoxTextBox}"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"
                                     Margin="{TemplateBinding Padding}"
                                     Focusable="True"
                                     Background="Transparent"
                                     Foreground="{TemplateBinding Foreground}"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource SurfaceColor}"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource BorderColor}"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True" Background="{DynamicResource SurfaceColor}">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                            <Trigger Property="IsEditable" Value="true">
                                <Setter Property="IsTabStop" Value="false"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- Ensure dropdown items match app colors -->
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="{x:Type ComboBoxItem}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                    <Border x:Name="ItemBorder" Background="{TemplateBinding Background}">
                                        <ContentPresenter Margin="4,2"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource MenuHoverColor}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Row -->
        <Border Grid.Row="0" Background="{DynamicResource TopBarColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid Margin="8,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Name -->
                <TextBlock Grid.Column="0" Text="Name" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" x:Name="NameTextBox" Height="28" Padding="8,4" Background="{DynamicResource SurfaceColor}" Foreground="{DynamicResource TextPrimaryColor}" BorderBrush="{DynamicResource BorderColor}"/>

                <!-- spacer -->
                <Grid Grid.Column="2"/>

                <!-- Size -->
                <TextBlock Grid.Column="3" Text="Size" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,8,0"/>
                <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBox x:Name="SizeXTextBox" Width="48" Height="24" Text="2" Padding="6,2" Background="{DynamicResource SurfaceColor}" Foreground="{DynamicResource TextPrimaryColor}" BorderBrush="{DynamicResource BorderColor}" Margin="0,0,4,0"/>
                    <TextBlock Text="X" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,4,0"/>
                    <TextBox x:Name="SizeYTextBox" Width="48" Height="24" Text="2" Padding="6,2" Background="{DynamicResource SurfaceColor}" Foreground="{DynamicResource TextPrimaryColor}" BorderBrush="{DynamicResource BorderColor}"/>
                </StackPanel>

                <!-- Edit size (pencil) -->
                <Button Grid.Column="5" x:Name="EditSizeButton" Content="✏️" Width="20" Height="20" Background="Transparent" BorderThickness="0" Click="EditSizeButton_Click" ToolTip="Edit rows/columns"/>

                <!-- Resolution -->
                <TextBlock Grid.Column="6" Text="Resolution" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryColor}" Margin="16,0,8,0"/>
                <ComboBox Grid.Column="7" x:Name="ResolutionCombo" Height="28" Background="{DynamicResource SurfaceColor}" Foreground="{DynamicResource TextPrimaryColor}" BorderBrush="{DynamicResource BorderColor}" Style="{StaticResource CompactComboBoxStyle}">
                    <ComboBoxItem>1920 x 1080</ComboBoxItem>
                    <ComboBoxItem>3840 x 2160</ComboBoxItem>
                    <ComboBoxItem>4096 x 2160</ComboBoxItem>
                </ComboBox>

                <!-- Auto-Bind -->
                <CheckBox Grid.Column="8" x:Name="AutoBindCheck" Content="Auto-Bind Output Channel" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryColor}" Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <!-- Size edit popup -->
        <Popup x:Name="EditSizePopup"
               PlacementTarget="{Binding ElementName=EditSizeButton}"
               Placement="Bottom"
               StaysOpen="False"
               AllowsTransparency="True">
            <Border Background="{DynamicResource SurfaceColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Row" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,6,0"/>
                    <TextBox Grid.Column="1" x:Name="PopupRowText" Height="24" Padding="6,2"/>
                    <Grid Grid.Column="2"/>
                    <TextBlock Grid.Column="3" Text="Column" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryColor}" Margin="0,0,6,0"/>
                    <TextBox Grid.Column="4" x:Name="PopupColText" Height="24" Padding="6,2"/>
                    <Grid Grid.Column="5"/>
                    <Button Grid.Column="6" Content="OK" Width="60" Height="26" Margin="0,0,6,0" Background="{DynamicResource PrimaryColor}" Foreground="White" BorderThickness="0" Click="SizePopupOk_Click"/>
                    <Button Grid.Column="7" Content="Cancel" Width="60" Height="26" Click="SizePopupCancel_Click"/>
                </Grid>
            </Border>
        </Popup>

        <!-- Body with Left Sidebar and 2x2 Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar -->
            <Border Grid.Column="0"
                    Background="{DynamicResource SidebarColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,0,1,0">
                <StackPanel Margin="8">
                    <TextBlock Text="Output Channel" 
                               Foreground="{DynamicResource TextPrimaryColor}" 
                               FontSize="12" 
                               Margin="2,4,0,6"/>
                    <ComboBox x:Name="OutputChannelCombo"
                              Height="28"
                              Background="{DynamicResource SurfaceColor}"
                              Foreground="{DynamicResource TextPrimaryColor}"
                              BorderBrush="{DynamicResource BorderColor}"
                              Style="{StaticResource CompactComboBoxStyle}">
                        <ComboBoxItem>Channel 1</ComboBoxItem>
                        <ComboBoxItem>Channel 2</ComboBoxItem>
                        <ComboBoxItem>Channel 3</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Border>

            <!-- Right Panel: 2x2 video wall preview -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="34"/>
                </Grid.RowDefinitions>

                <!-- Dynamic preview grid -->
                <Grid x:Name="PreviewGrid" Grid.Row="0" Background="{DynamicResource CardBackgroundColor}" Margin="0"/>

                <!-- Bottom icons bar -->
                <Border Grid.Row="1" Background="{DynamicResource TopBarColor}" Margin="0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,2">
                        <Button Content="⧉" Width="28" Height="24" Background="Transparent" BorderThickness="0" ToolTip="Action 1"/>
                        <Button Content="🔗" Width="28" Height="24" Background="Transparent" BorderThickness="0" ToolTip="Action 2" Margin="4,0,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" Background="{DynamicResource TopBarColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1,1,1,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="12" >
                <Button Content="OK" Width="90" Height="28" Margin="0,0,8,0" Background="{DynamicResource PrimaryColor}" Foreground="White" BorderThickness="0" Click="Save_Click"/>
                <Button Content="Cancel" Width="90" Height="28" Click="Cancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

