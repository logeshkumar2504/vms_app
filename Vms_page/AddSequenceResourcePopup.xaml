<Window x:Class="Vms_page.AddSequenceResourcePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sequence Resource Info" Height="620" Width="1100"
        Background="{DynamicResource BackgroundColor}"
        WindowStyle="SingleBorderWindow" ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="36"/>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="CellStyle" TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Row (matches screenshot top content area) -->
        <Border Grid.Row="0" Background="{DynamicResource SurfaceColor}" Padding="12" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Name" Style="{StaticResource LabelText}" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" x:Name="NameBox" Style="{StaticResource InputBox}"/>
                <TextBlock Grid.Column="2" Text="Interval(s)" Style="{StaticResource LabelText}" Margin="16,0,8,0"/>
                <TextBox Grid.Column="3" x:Name="IntervalBox" Style="{StaticResource InputBox}" Text="20" PreviewTextInput="IntervalBox_PreviewTextInput"/>
                <Button Grid.Column="4" x:Name="DeleteBtn" Content="Delete" Style="{StaticResource ToolbarButton}" Margin="12,0,0,0" Click="DeleteBtn_Click"/>
            </Grid>
        </Border>

        <!-- Main split area -->
        <Grid Grid.Row="1" Margin="0,8,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Video Channel + search -->
            <Border Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Margin="0,-10,12,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="465.08"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Video Channel" Style="{StaticResource LabelText}" Margin="12,10,12,6"/>
                    <Border Grid.Row="1" Margin="12,0,12,8" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Height="28">
                        <Grid>
                            <TextBlock Text="🔍" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{DynamicResource TextSecondaryColor}"/>
                            <TextBox x:Name="ChannelSearchBox" Style="{StaticResource InputBox}" Margin="22,0,0,0" BorderThickness="0" Background="Transparent" Foreground="{DynamicResource TextSecondaryColor}" Text="Enter Keywords"/>
                        </Grid>
                    </Border>
                    <!-- Placeholder list -->
                    <Border Grid.Row="2" Margin="0,0,0,12" Background="{DynamicResource BackgroundColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1"/>
                </Grid>
            </Border>

            <!-- Middle: arrow button -->
            <Grid Grid.Column="1">
                <Button Content=">>" Width="36" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource ToolbarButton}"/>
            </Grid>

            <!-- Right: table -->
            <Border Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Margin="0,0,12,0">
                <DataGrid x:Name="ChannelGrid" AutoGenerateColumns="False" Style="{StaticResource DataGridStyle}" ColumnHeaderStyle="{StaticResource HeaderStyle}" CellStyle="{StaticResource CellStyle}" Margin="-73,-11,-11,-1">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="" Width="40">
                            <DataGridTemplateColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.HeaderTemplate>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Channel Name" Binding="{Binding ChannelName}" Width="2*"/>
                        <DataGridTextColumn Header="Stream" Binding="{Binding Stream}" Width="*"/>
                        <DataGridTextColumn Header="Preset" Binding="{Binding Preset}" Width="*"/>
                        <DataGridTemplateColumn Header="Operation" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Edit" Style="{StaticResource ToolbarButton}" Padding="8,2" Margin="0,0,6,0"/>
                                        <Button Content="Delete" Style="{StaticResource ToolbarButton}" Padding="8,2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Remarks section -->
        <Border Grid.Row="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1,1,1,1" Margin="0,369,0,8">
            <Grid Margin="-12,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Remarks:" Style="{StaticResource LabelText}" Margin="20,0,10,0"/>
                <TextBox Grid.Row="1" x:Name="RemarksBox" Style="{StaticResource InputBox}" Margin="10,5,0,15" BorderThickness="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
            </Grid>
        </Border>

        <!-- Bottom buttons -->
        <Border Grid.Row="1" Background="{DynamicResource SurfaceColor}" Padding="12" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1,1,1,1" Margin="0,477,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="OkBtn" Content="OK" Width="80" Height="28" Margin="0,0,8,0" Background="{DynamicResource PrimaryColor}" Foreground="{DynamicResource TextPrimaryColor}" BorderThickness="0" Click="OkBtn_Click"/>
                <Button x:Name="CancelBtn" Content="Cancel" Width="80" Height="28" Background="{DynamicResource SurfaceColor}" Foreground="{DynamicResource TextPrimaryColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Click="CancelBtn_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

