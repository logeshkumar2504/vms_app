<Window x:Class="Vms_page.VideoWallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Video Wall" Height="600" Width="1000"
        Background="{DynamicResource BackgroundColor}"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- Custom Native TopBar Style -->
        <Style x:Key="NativeTopBarStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource TopBarColor}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="1" Opacity="0.1" BlurRadius="4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Popup Button Style -->
        <Style x:Key="PopupButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource TextSecondaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Shadow Effect -->
        <DropShadowEffect x:Key="CardShadow" 
                          Color="#000000" 
                          Direction="270" 
                          ShadowDepth="3" 
                          Opacity="0.3" 
                          BlurRadius="10"/>

        <!-- Primary (filled) button style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MenuHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SecondaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary (outlined) button style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MenuHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Compact popup menu item style -->
        <Style x:Key="PopupMenuItemStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="MinHeight" Value="26"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter Margin="6,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MenuHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <!-- Native TopBar with Dark Mode Toggle -->
        <Border DockPanel.Dock="Top" 
                Height="35"
                Style="{StaticResource NativeTopBarStyle}">
            <Grid>
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Left" 
                           VerticalAlignment="Center"
                           Margin="15,0">
                    <!-- Warning/Alert Icon -->
                    <TextBlock Text="⚠️" 
                               FontSize="18" 
                               Foreground="{DynamicResource TextPrimaryColor}"
                               Margin="0,0,12,0"/>

                    <!-- Notification/Bell Icon -->
                    <TextBlock Text="🔔" 
                               FontSize="18" 
                               Foreground="{DynamicResource TextPrimaryColor}"
                               Margin="0,0,12,0"/>

                    <!-- Download/Arrow Icon -->
                    <TextBlock Text="⬇️" 
                               FontSize="18" 
                               Foreground="{DynamicResource TextPrimaryColor}"
                               Margin="0,0,12,0"/>

                    <!-- Clipboard/List Icon -->
                    <TextBlock Text="📋" 
                               FontSize="18" 
                               Foreground="{DynamicResource TextPrimaryColor}"
                               Margin="0,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Right" 
                           VerticalAlignment="Center" 
                           Margin="15,0">
                    <!-- Lock Icon -->
                    <Button x:Name="LockButton"
                            Width="24" Height="24"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="LockButton_Click"
                            ToolTip="Lock/Unlock"
                            Margin="0,0,8,0">
                        <TextBlock Text="🔒" 
                                   FontSize="16" 
                                   Foreground="{DynamicResource TextPrimaryColor}"/>
                    </Button>

                    <!-- Menu Icon -->
                    <Button x:Name="MainMenuButton"
                            Width="24" Height="24"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="MainMenuButton_Click"
                            ToolTip="Menu"
                            Margin="0,0,8,0">
                        <TextBlock Text="☰" 
                                   FontSize="16" 
                                   Foreground="{DynamicResource TextPrimaryColor}"/>
                    </Button>

                    <!-- Theme Toggle Button -->
                    <Button x:Name="ThemeButton"
                            Width="24" Height="24"
                            Background="Transparent"
                            BorderThickness="0"
                            Click="ThemeButton_Click"
                            ToolTip="Select Theme">
                        <TextBlock Text="🌙" 
                                   FontSize="16" 
                                   Foreground="{DynamicResource TextPrimaryColor}"/>
                    </Button>

                    <!-- Theme Options Popup -->
                    <Popup x:Name="ThemePopup"
                           PlacementTarget="{Binding ElementName=ThemeButton}"
                           Placement="Bottom"
                           IsOpen="False"
                           AllowsTransparency="True"
                           PopupAnimation="Slide">
                        <Border Background="{DynamicResource SurfaceColor}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8"
                                Effect="{DynamicResource CardShadow}">
                            <StackPanel Orientation="Vertical" MinWidth="100">
                                <Button Content="☀️ Light Mode" 
                                        Style="{StaticResource PopupButtonStyle}"
                                        Click="LightMode_Click"
                                        Margin="0,2"/>
                                <Button Content="🌙 Dark Mode" 
                                        Style="{StaticResource PopupButtonStyle}"
                                        Click="DarkMode_Click"
                                        Margin="0,2"/>
                            </StackPanel>
                        </Border>
                    </Popup>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Separator Line -->
        <Border Height="1" 
                Background="{DynamicResource BorderColor}" 
                Margin="0,0,0,0"
                Opacity="0.6"/>

        <!-- Main Content Grid -->
        <Grid Margin="0,12,0,12" Background="{DynamicResource BackgroundColor}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Navbar -->
            <Border Grid.Row="0"
                    Background="{DynamicResource TopBarColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,0,0,1">
                <Grid Margin="16,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                <!-- Left Controls: Operation + Screen Control (both buttons) -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="OperationButton"
                            Content="Operation"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="OperationButton_Click"/>

                    <Button x:Name="ScreenControlButton"
                            Content="Screen Control"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="ScreenControlButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Centered Add Video Wall button (only shown for Operation) -->
                <Button x:Name="AddVideoWallButton"
                        Visibility="Collapsed"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Padding="24,12"
                        MinWidth="220"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="AddVideoWallButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock Text="Video Wall"/>
                    </StackPanel>
                </Button>

                <!-- Popup for Add Video Wall options -->
                <Popup x:Name="AddVideoWallPopup"
                       PlacementTarget="{Binding ElementName=AddVideoWallButton}"
                       Placement="Bottom"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       IsOpen="False">
                    <Border Background="{DynamicResource SurfaceColor}"
                            BorderBrush="{DynamicResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="6">
                        <StackPanel MinWidth="200">
                            <Button Content="Add Dx Video Wall"
                                    Style="{StaticResource PopupMenuItemStyle}"
                                    Margin="0,2"
                                    Click="AddDxVideoWall_Click"/>
                            <Button Content="Add Decoding Card Video Wall"
                                    Style="{StaticResource PopupMenuItemStyle}"
                                    Margin="0,2"
                                    Click="AddDecodingCardVideoWall_Click"/>
                        </StackPanel>
                    </Border>
                </Popup>
            </Grid>
        </Grid>
        </Grid>
    </DockPanel>
</Window>

