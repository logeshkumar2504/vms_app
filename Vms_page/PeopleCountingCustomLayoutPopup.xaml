    <Window x:Class="Vms_page.PeopleCountingCustomLayoutPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Layout Designer" Height="600" Width="800"
        Background="{DynamicResource BackgroundColor}" 
        WindowStyle="SingleBorderWindow" 
        ResizeMode="CanResize"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner"
        Topmost="True"
        MinHeight="500" MinWidth="700">

    <Window.Resources>
        <!-- Button Styles -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource WarningColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource WarningColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonPressedColor}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OKButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonPressedColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MenuHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SurfaceColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Control Bar -->
        <Border Grid.Row="0" 
                Background="{DynamicResource CardBackgroundColor}" 
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="0,0,0,1"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Add Button -->
                <Button Grid.Column="0" 
                        Style="{StaticResource ActionButtonStyle}"
                        Click="AddButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="14" Margin="0,0,4,0"/>
                        <TextBlock Text="Add"/>
                    </StackPanel>
                </Button>

                <!-- Delete Button -->
                <Button Grid.Column="1" 
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="8,0,0,0"
                        Click="DeleteButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🗑" FontSize="12" Margin="0,0,4,0"/>
                        <TextBlock Text="Delete"/>
                    </StackPanel>
                </Button>

                <!-- Layout Input Fields -->
                <StackPanel Grid.Column="2" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center"
                            Margin="16,0,0,0">
                    <TextBlock Text="Layout" 
                               Foreground="{DynamicResource TextPrimaryColor}" 
                               FontSize="12" 
                               FontWeight="Medium"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <TextBox x:Name="RowsTextBox" 
                             Width="50" 
                             Height="24"
                             Text="4"
                             FontSize="12"
                             Foreground="{DynamicResource TextPrimaryColor}"
                             Background="{DynamicResource SurfaceColor}"
                             BorderBrush="{DynamicResource WarningColor}"
                             BorderThickness="1"
                             TextAlignment="Center"
                             VerticalAlignment="Center"
                             TextChanged="LayoutTextChanged"/>
                    <TextBlock Text="X" 
                               Foreground="{DynamicResource TextPrimaryColor}" 
                               FontSize="12" 
                               VerticalAlignment="Center"
                               Margin="8,0"/>
                    <TextBox x:Name="ColsTextBox" 
                             Width="50" 
                             Height="24"
                             Text="4"
                             FontSize="12"
                             Foreground="{DynamicResource TextPrimaryColor}"
                             Background="{DynamicResource SurfaceColor}"
                             BorderBrush="{DynamicResource WarningColor}"
                             BorderThickness="1"
                             TextAlignment="Center"
                             VerticalAlignment="Center"
                             TextChanged="LayoutTextChanged"/>
                </StackPanel>

                <!-- Combine Button -->
                <Button Grid.Column="3" 
                        Content="Combine" 
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="16,0,0,0"
                        Click="CombineButton_Click"/>

                <!-- Undo Button -->
                <Button Grid.Column="4" 
                        Content="Undo" 
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="8,0,0,0"
                        Click="UndoButton_Click"/>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar with Preview -->
            <Border Grid.Column="0" 
                    Width="120"
                    Background="{DynamicResource CardBackgroundColor}" 
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="0,0,1,0"
                    Padding="12">
                <StackPanel>
                    <!-- Preview Icon -->
                    <Grid x:Name="PreviewIcon" Width="80" Height="60" Margin="0,0,0,8"/>

                    <!-- Layout Number -->
                </StackPanel>
            </Border>

            <!-- Main Grid Designer -->
            <Border Grid.Column="1" 
                    Background="{DynamicResource SurfaceColor}"
                    BorderBrush="{DynamicResource WarningColor}"
                    BorderThickness="1"
                    Margin="0">
                <Grid x:Name="MainGridDesigner" 
                      Margin="30">
                    <!-- Grid will be populated dynamically -->
                </Grid>
            </Border>
        </Grid>

        <!-- Bottom Instructions and Buttons -->
        <Border Grid.Row="2" 
                Background="{DynamicResource CardBackgroundColor}" 
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="0,1,0,0"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Instructions -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="Instructions:" 
                               Foreground="{DynamicResource TextPrimaryColor}" 
                               FontSize="12" 
                               FontWeight="Bold"
                               Margin="0,0,0,2"/>
                    <TextBlock Text="• Click cells to select them" 
                               Foreground="{DynamicResource TextSecondaryColor}" 
                               FontSize="11"/>
                    <TextBlock Text="• Click 'Combine' to merge selected cells" 
                               Foreground="{DynamicResource TextSecondaryColor}" 
                               FontSize="11"/>
                    <TextBlock Text="• Use input fields to change grid size" 
                               Foreground="{DynamicResource TextSecondaryColor}" 
                               FontSize="11"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal">
                    <Button Content="OK" 
                            Style="{StaticResource OKButtonStyle}"
                            Click="OK_Click"
                            Margin="0,0,8,0"/>
                    <Button Content="Cancel" 
                            Style="{StaticResource CancelButtonStyle}"
                            Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
