<Window x:Class="Vms_page.RecordingScheduleWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="Recording Schedule" Height="720" Width="1100" MinHeight="500" MinWidth="900"
	WindowStartupLocation="CenterOwner"
	Background="#1E1E1E">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBg" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SidebarBg" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="BorderBrushDark" Color="#333333"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B0B0B0"/>
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#2F2F2F"/>
            <Setter Property="BorderBrush" Value="#3C3C3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#404040"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="HourBlockTemplate">
            <Border Background="#222" BorderBrush="#3A3A3A" BorderThickness="1" Width="24" Height="16" Margin="2"/>
        </DataTemplate>
        
        <!-- Modern Button Styles -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#64B5F6"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#42A5F5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1E88E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#B0B0B0"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#404040"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Camera Item Style -->
        <Style x:Key="CameraItemStyle" TargetType="Border">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Modern Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBg}" BorderBrush="{StaticResource BorderBrushDark}" BorderThickness="0,0,1,0" Margin="0" MinWidth="260">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header Section -->
                <Border Grid.Row="0" Background="#1A1A1A" Padding="16,12" Margin="0,0,0,8">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <Image Source="/Assets/Icons/Record-Schedule.png" Width="20" Height="20" Margin="0,0,8,0" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Text="Recording Schedule" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="Configure recording schedules and timings" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   FontSize="10"/>
                    </StackPanel>
                </Border>
                
                <!-- Enhanced Search Section -->
                <Border Grid.Row="1" Margin="12,0,12,16" Background="#2A2A2A" BorderBrush="#404040" BorderThickness="1" CornerRadius="6">
                    <Grid Height="40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Search Icon -->
                        <Border Grid.Column="0" Width="40" VerticalAlignment="Center">
                            <TextBlock Text="🔍" 
                                       FontSize="14" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center" 
                                       Foreground="#64B5F6"/>
                        </Border>
                        
                        <!-- Search Input -->
                        <TextBox x:Name="KeywordTextBox" 
                                 Grid.Column="1" 
                                 BorderThickness="0" 
                                 Background="Transparent" 
                                 Foreground="#FFFFFF" 
                                 Padding="0,8,8,8" 
                                 CaretBrush="#64B5F6" 
                                 Text="Search schedules..." 
                                 FontSize="12"
                                 GotFocus="KeywordTextBox_GotFocus" 
                                 LostFocus="KeywordTextBox_LostFocus"
                                 VerticalAlignment="Center"/>
                        
                        <!-- Clear Button -->
                        <Button Grid.Column="2" 
                                Width="24" Height="24" 
                                Margin="0,0,8,0"
                                Background="Transparent" 
                                BorderThickness="0"
                                VerticalAlignment="Center">
                            <TextBlock Text="✕" 
                                       FontSize="12" 
                                       Foreground="#888888" 
                                       HorizontalAlignment="Center"/>
                        </Button>
                    </Grid>
                </Border>
                
                <!-- Camera List Section -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="12,0,12,0">
                        <!-- Category Header -->
                        <Border Background="#333333" CornerRadius="4" Margin="0,0,0,8" Padding="12,8">
                            <TextBlock Text="Recording Templates" 
                                       Foreground="{StaticResource TextSecondary}" 
                                       FontSize="11" 
                                       FontWeight="SemiBold"/>
                        </Border>
                        
                        <!-- Recording Schedule Templates -->
                        <StackPanel>
                            <Border Style="{StaticResource CameraItemStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Width="32" Height="24" Background="#4CAF50" CornerRadius="3" Margin="0,0,8,0">
                                        <Image Source="/Assets/Icons/Record-Schedule.png" Width="16" Height="16" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="24/7 Recording" 
                                                   Foreground="#FFFFFF" 
                                                   FontSize="12" 
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="Continuous recording" 
                                                   Foreground="#B0B0B0" 
                                                   FontSize="10"/>
                                    </StackPanel>
                                    
                                    <Border Grid.Column="2" 
                                            Width="8" Height="8" 
                                            Background="#4CAF50" 
                                            CornerRadius="4"/>
                                </Grid>
                            </Border>
                            
                            <Border Style="{StaticResource CameraItemStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Width="32" Height="24" Background="#FF9800" CornerRadius="3" Margin="0,0,8,0">
                                        <Image Source="/Assets/Icons/Record-Schedule.png" Width="16" Height="16" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Business Hours" 
                                                   Foreground="#FFFFFF" 
                                                   FontSize="12" 
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="9 AM - 6 PM" 
                                                   Foreground="#B0B0B0" 
                                                   FontSize="10"/>
                                    </StackPanel>
                                    
                                    <Border Grid.Column="2" 
                                            Width="8" Height="8" 
                                            Background="#FF9800" 
                                            CornerRadius="4"/>
                                </Grid>
                            </Border>
                            
                            <Border Style="{StaticResource CameraItemStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Width="32" Height="24" Background="#F44336" CornerRadius="3" Margin="0,0,8,0">
                                        <Image Source="/Assets/Icons/Record-Schedule.png" Width="16" Height="16" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Motion Detection" 
                                                   Foreground="#FFFFFF" 
                                                   FontSize="12" 
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="Event-based recording" 
                                                   Foreground="#B0B0B0" 
                                                   FontSize="10"/>
                                    </StackPanel>
                                    
                                    <Border Grid.Column="2" 
                                            Width="8" Height="8" 
                                            Background="#F44336" 
                                            CornerRadius="4"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Footer Actions -->
                <Border Grid.Row="3" 
                        Background="#1A1A1A" 
                        BorderBrush="#333333" 
                        BorderThickness="0,1,0,0" 
                        Padding="12,8">
                    <StackPanel>
                        <Button Style="{StaticResource ModernButtonStyle}"
                                Content="New Template"
                                Margin="0,0,0,4"/>
                        <Button Style="{StaticResource SecondaryButtonStyle}"
                                Content="Import Schedule"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Main content -->
        <Grid Background="{StaticResource PanelBg}" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Enable -->
            <CheckBox Grid.Row="0" Content="Enable" Foreground="{StaticResource TextSecondary}" Margin="16,14,16,8"/>

            <!-- Toolbar -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="16,0,16,12">
                <Button Style="{StaticResource ToolbarButton}" Content="Select" Width="57"/>
                <Button Style="{StaticResource ToolbarButton}" Content="Edit Template" Width="102"/>
                <Button Style="{StaticResource ToolbarButton}" Content="Clear" Width="66"/>
            </StackPanel>

            <!-- Schedule block -->
            <ScrollViewer Grid.Row="2" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Border Background="#232323" BorderBrush="#333" BorderThickness="1">
                    <Grid Margin="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Hour header every 2 hours 0..24 -->
                        <Grid Grid.Row="0" Margin="110,0,0,6">
                            <UniformGrid Columns="13">
                                <TextBlock Text="0" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Left"/>
                                <TextBlock Text="2" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="4" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="6" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="8" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="10" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="14" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="16" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="18" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="20" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="22" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock Text="24" Foreground="{StaticResource TextSecondary}"/>
                            </UniformGrid>
                        </Grid>

                        <!-- Day rows -->
                        <StackPanel Grid.Row="1">
                            <!-- Helper: a row template replicated for each day -->
                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Width="110" Text="Monday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Width="110" Text="Tuesday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Width="110" Text="Wednesday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Width="110" Text="Thursday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Width="110" Text="Friday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <TextBlock Width="110" Text="Saturday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Width="110" Text="Sunday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Width="110" Text="Holiday" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding HourBlocks}" ItemTemplate="{StaticResource HourBlockTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="24"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>
            </ScrollViewer>

            <!-- Footer actions -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
                <Button Style="{StaticResource ToolbarButton}" Content="Save" Width="89"/>
                <Button Style="{StaticResource ToolbarButton}" Content="Copy To" Width="74" Margin="12,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

