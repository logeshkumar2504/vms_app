<Window x:Class="Vms_page.LiveViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Live View" Height="700" Width="1200"
        Background="{DynamicResource BackgroundColor}" 
        WindowStyle="SingleBorderWindow" 
        ResizeMode="CanResize"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Custom Native TopBar Style -->
        <Style x:Key="NativeTopBarStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource TopBarColor}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="1" Opacity="0.1" BlurRadius="4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Popup Button Style -->
        <Style x:Key="PopupButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="2"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource TextSecondaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Shadow Effect -->
        <DropShadowEffect x:Key="CardShadow" 
                          Color="#000000" 
                          Direction="270" 
                          ShadowDepth="3" 
                          Opacity="0.3" 
                          BlurRadius="10"/>

        <!-- Custom ScrollBar Style -->
        <Style x:Key="ThinScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Background="{DynamicResource TextSecondaryColor}" 
                                           BorderThickness="0">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderThickness="0"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Scroll Style -->
        <Style x:Key="ThinScrollStyle" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0" 
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  CanContentScroll="{TemplateBinding CanContentScroll}"
                                                  CanHorizontallyScroll="False"
                                                  CanVerticallyScroll="False"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                                       Grid.Column="1" Grid.Row="0"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Style="{StaticResource ThinScrollBarStyle}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar" 
                                       Grid.Column="0" Grid.Row="1"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                       Style="{StaticResource ThinScrollBarStyle}"
                                       Orientation="Horizontal"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Grid Layout Button Style -->
        <Style x:Key="GridLayoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MenuHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource WarningColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <!-- Main Content Grid -->
        <Grid Margin="0,12,0,12" Background="{DynamicResource BackgroundColor}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

        <!-- Left Sidebar -->
        <Border Grid.Column="0" 
                x:Name="Sidebar"
                Background="{DynamicResource SidebarColor}" 
                BorderBrush="{DynamicResource BorderColor}" 
                BorderThickness="0,0,1,0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Empty Top Section -->
                <Grid Grid.Row="0" 
                      Background="{DynamicResource SidebarColor}">
                </Grid>

                <!-- Action Icons and Search Bar -->
                <Grid Grid.Row="1" Grid.RowSpan="3" Margin="8,8,8,8">
                    <StackPanel>

                        <!-- Action Icons - Above search bar -->
                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Left"
                                    Margin="0,0,0,8">

                            <!-- Add Icon -->
                            <Border Background="Transparent" 
                                    CornerRadius="4" 
                                    Margin="0,0,6,0"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="AddIcon_Click"
                                    ToolTip="Add">
                                <TextBlock Text="+" 
                                           FontSize="12" 
                                           Foreground="{DynamicResource TextSecondaryColor}"
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"
                                           Padding="3"/>
                            </Border>

                            <!-- Edit Icon -->
                            <Border Background="Transparent" 
                                    CornerRadius="4" 
                                    Margin="0,0,6,0"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="EditIcon_Click"
                                    ToolTip="Edit">
                                <TextBlock Text="✏️" 
                                           FontSize="12" 
                                           Foreground="{DynamicResource TextSecondaryColor}"
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"
                                           Padding="3"/>
                            </Border>

                            <!-- Delete Icon -->
                            <Border Background="Transparent" 
                                    CornerRadius="4" 
                                    Cursor="Hand"
                                    MouseLeftButtonDown="DeleteIcon_Click"
                                    ToolTip="Delete">
                                <TextBlock Text="🗑️" 
                                           FontSize="12" 
                                           Foreground="{DynamicResource TextSecondaryColor}"
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"
                                           Padding="3"/>
                            </Border>
                        </StackPanel>

                        <!-- Search Bar -->
                        <Border Background="{DynamicResource CardBackgroundColor}" 
                                CornerRadius="4" 
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="🔍" 
                                           FontSize="12" 
                                           Foreground="{DynamicResource TextSecondaryColor}"
                                           VerticalAlignment="Center" 
                                           Margin="8,0,4,0"/>

                                <TextBox Grid.Column="1" 
                                         Text="Enter Keywords" 
                                         Foreground="{DynamicResource TextSecondaryColor}" 
                                         Background="Transparent"
                                         BorderThickness="0"
                                         FontSize="12"
                                         VerticalAlignment="Center"
                                         Margin="0,4,8,4"
                                         x:Name="SearchTextBox"
                                         GotFocus="SearchTextBox_GotFocus"
                                         LostFocus="SearchTextBox_LostFocus"/>
                            </Grid>
                        </Border>

                        <!-- Navigation Icons -->
                        <StackPanel Orientation="Vertical" 
                                    HorizontalAlignment="Left" 
                                    VerticalAlignment="Top"
                                    Margin="0,8,0,0">

                            <!-- Video Icon -->
                            <Border Background="Transparent" 
                                    CornerRadius="4" 
                                    Margin="0,0,0,8"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="VideoIcon_Click"
                                    MouseEnter="VideoIcon_MouseEnter"
                                    MouseLeave="VideoIcon_MouseLeave"
                                    ToolTip="Video"
                                    Width="130"
                                    Height="32"
                                    x:Name="VideoIconBorder">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="32"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="🎥" 
                                               FontSize="18" 
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="1"
                                               Text="Video Channel" 
                                               FontSize="10" 
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Opacity="0"
                                               x:Name="VideoLabel"/>
                                </Grid>
                            </Border>

                            <!-- View Icon -->
                            <Border Background="Transparent" 
                                    CornerRadius="4" 
                                    Margin="0,0,0,8"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="ViewIcon_Click"
                                    MouseEnter="ViewIcon_MouseEnter"
                                    MouseLeave="ViewIcon_MouseLeave"
                                    ToolTip="View"
                                    Width="130"
                                    Height="32"
                                    x:Name="ViewIconBorder">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="32"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="⊞" 
                                               FontSize="16" 
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="1"
                                               Text="View" 
                                               FontSize="10" 
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Opacity="0"
                                               x:Name="ViewLabel"/>
                                </Grid>
                            </Border>

                            <!-- Sequence Resources Icon -->
                            <Border Background="Transparent" 
                                    CornerRadius="4" 
                                    Cursor="Hand"
                                    MouseLeftButtonDown="SequenceResourcesIcon_Click"
                                    MouseEnter="SequenceResourcesIcon_MouseEnter"
                                    MouseLeave="SequenceResourcesIcon_MouseLeave"
                                    ToolTip="Sequence Resources"
                                    Width="130"
                                    Height="32"
                                    x:Name="SequenceResourcesIconBorder">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="32"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="🔄" 
                                               FontSize="18" 
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock Grid.Column="1"
                                               Text="Sequence Resource" 
                                               FontSize="10" 
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Opacity="0"
                                               x:Name="SequenceResourcesLabel"/>
                                </Grid>
                            </Border>
                        </StackPanel>


                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Right Panel - Main Content Area -->
        <Border Grid.Column="1" 
                Background="{DynamicResource CardBackgroundColor}">

            <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>



                <!-- Content Area with Video Grid -->
                <Grid Grid.Row="1" Margin="0" x:Name="MainContentArea">

                    <!-- Video Grid View -->
                    <Grid x:Name="VideoView" Visibility="Visible">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 2x2 Grid for Video Feeds -->
                        <Grid Grid.Row="0" Margin="0" x:Name="MainVideoGrid">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Top Left Video Feed -->
                            <Border Grid.Row="0" Grid.Column="0" 
                                    Background="{DynamicResource SurfaceColor}" 
                                    CornerRadius="0" 
                                    Margin="0"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource BorderColor}"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="VideoFeed_Click">
                                <Grid>
                                    <!-- Empty grid - no camera icon -->
                                </Grid>
                            </Border>

                            <!-- Top Right Video Feed -->
                            <Border Grid.Row="0" Grid.Column="1" 
                                    Background="{DynamicResource SurfaceColor}" 
                                    CornerRadius="0" 
                                    Margin="0"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource BorderColor}"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="VideoFeed_Click">
                                <Grid>
                                    <!-- Empty grid - no camera icon -->
                                </Grid>
                            </Border>

                            <!-- Bottom Left Video Feed -->
                            <Border Grid.Row="1" Grid.Column="0" 
                                    Background="{DynamicResource SurfaceColor}" 
                                    CornerRadius="0" 
                                    Margin="0"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource BorderColor}"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="VideoFeed_Click"
                                    x:Name="BottomLeftFeed">
                                <Grid>
                                    <!-- CCTV Camera Icon -->
                                    <Border Width="60" Height="60" 
                                            Background="Transparent"
                                            BorderBrush="{DynamicResource TextSecondaryColor}"
                                            BorderThickness="2"
                                            CornerRadius="30">
                                        <Grid>
                                            <!-- Camera Body -->
                                            <Rectangle Width="24" Height="16" 
                                                       Fill="{DynamicResource TextSecondaryColor}" 
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       RadiusX="2" RadiusY="2"/>
                                            <!-- Camera Lens -->
                                            <Ellipse Width="8" Height="8" 
                                                     Fill="{DynamicResource TextSecondaryColor}" 
                                                     HorizontalAlignment="Center" 
                                                     VerticalAlignment="Center"
                                                     Margin="-8,0,0,0"/>
                                            <!-- Mounting Arm -->
                                            <Path Data="M 8,8 L 12,12 L 12,16 L 8,16 Z" 
                                                  Fill="{DynamicResource TextSecondaryColor}" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  Margin="8,8,0,0"/>
                                        </Grid>
                        </Border>
                                </Grid>
        </Border>

                            <!-- Bottom Right Video Feed (Selected) -->
                            <Border Grid.Row="1" Grid.Column="1" 
                                    Background="{DynamicResource SurfaceColor}" 
                                    CornerRadius="0" 
                                    Margin="0"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource WarningColor}"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="VideoFeed_Click"
                                    x:Name="BottomRightFeed">
                                <Grid>
                                    <!-- CCTV Camera Icon -->
                                    <Border Width="60" Height="60" 
                                            Background="Transparent"
                                            BorderBrush="{DynamicResource WarningColor}"
                                            BorderThickness="2"
                                            CornerRadius="30">
                                        <Grid>
                                            <!-- Camera Body -->
                                            <Rectangle Width="24" Height="16" 
                                                       Fill="{DynamicResource WarningColor}" 
                                                       HorizontalAlignment="Center" 
                                                       VerticalAlignment="Center"
                                                       RadiusX="2" RadiusY="2"/>
                                            <!-- Camera Lens -->
                                            <Ellipse Width="8" Height="8" 
                                                     Fill="{DynamicResource WarningColor}" 
                                                     HorizontalAlignment="Center" 
                                                     VerticalAlignment="Center"
                                                     Margin="-8,0,0,0"/>
                                            <!-- Mounting Arm -->
                                            <Path Data="M 8,8 L 12,12 L 12,16 L 8,16 Z" 
                                                  Fill="{DynamicResource WarningColor}" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  Margin="8,8,0,0"/>
                                        </Grid>
                                    </Border>
                </Grid>
            </Border>
        </Grid>

                        <!-- Bottom Controls for Video View -->
                        <Border Grid.Row="1" 
                                Background="Transparent" 
                                Padding="8,2,8,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Left Side - Action Icons -->
                                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <!-- Grid Layout Button (moved to first position) -->
                                    <Button x:Name="GridLayoutButton" 
                                            Content="⚏" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,4,0"
                                            Cursor="Hand"
                                            Click="GridLayout_Click"
                                            ToolTip="Change grid layout"/>

                                    <!-- Save View Icon -->
                                    <Button x:Name="SaveViewButton" 
                                            Content="💾" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,4,0"
                                            Cursor="Hand"
                                            Click="SaveViewButton_Click"
                                            ToolTip="Save View"/>

                                    <!-- Close All Windows Icon -->
                                    <Button x:Name="CloseAllWindowsButton" 
                                            Content="🗙" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,4,0"
                                            Cursor="Hand"
                                            Click="CloseAllWindowsButton_Click"
                                            ToolTip="Close All Windows"/>

                                    <!-- Snapshot All Icon -->
                                    <Button x:Name="SnapshotAllButton" 
                                            Content="📸" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,4,0"
                                            Cursor="Hand"
                                            Click="SnapshotAllButton_Click"
                                            ToolTip="Snapshot All"/>

                                    <!-- Start Recording All Icon -->
                                    <Button x:Name="StartRecordingAllButton" 
                                            Content="🔴" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,4,0"
                                            Cursor="Hand"
                                            Click="StartRecordingAllButton_Click"
                                            ToolTip="Start Recording All"/>

                                    <!-- Stop Recording All Icon -->
                                    <Button x:Name="StopRecordingAllButton" 
                                            Content="⏹" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,4,0"
                                            Cursor="Hand"
                                            Click="StopRecordingAllButton_Click"
                                            ToolTip="Stop Recording All"/>

                                    <!-- Broadcast List Icon -->
                                    <Button x:Name="BroadcastListButton" 
                                            Content="📡" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,6,0"
                                            Cursor="Hand"
                                            Click="BroadcastListButton_Click"
                                            ToolTip="Broadcast List"/>

                                    <!-- Grid Layout Dropdown Popup -->
                                    <Popup x:Name="GridLayoutDropdownPopup" 
                                           PlacementTarget="{Binding ElementName=GridLayoutButton}" 
                                           Placement="Top" 
                                           IsOpen="False" 
                                           AllowsTransparency="True"
                                           StaysOpen="False">
                                        <Border Background="{DynamicResource CardBackgroundColor}" 
                                                CornerRadius="4" 
                                                BorderBrush="{DynamicResource BorderColor}" 
                                                BorderThickness="1" 
                                                Padding="16" 
                                                Margin="0,0,0,8" 
                                                MaxWidth="600" 
                                                MaxHeight="400">
                                            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                                          HorizontalScrollBarVisibility="Auto">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                                    <!-- Custom Section -->
                                                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                                                        <TextBlock Text="Custom" 
                                                                   Foreground="{DynamicResource TextPrimaryColor}" 
                                                                   FontSize="12" 
                                                                   FontWeight="Bold" 
                                                                   Margin="0,0,0,8"/>
                                                        <Button Content="✏️ Edit" 
                                                                Background="Transparent" 
                                                                Foreground="{DynamicResource TextPrimaryColor}" 
                                                                BorderThickness="0" 
                                                                FontSize="11" 
                                                                Padding="8,4" 
                                                                HorizontalAlignment="Left"
                                                                Cursor="Hand"
                                                                Click="CustomLayout_Click"/>
                                        </StackPanel>

                                                    <!-- Default Section -->
                                                    <StackPanel Grid.Row="1" Margin="0,0,0,12">
                                                        <TextBlock Text="Default" 
                                                                   Foreground="{DynamicResource TextPrimaryColor}" 
                                                                   FontSize="12" 
                                                                   FontWeight="Bold" 
                                                                   Margin="0,0,0,8"/>

                                                        <!-- Grid Layout Options -->
                                                        <UniformGrid Columns="9" Rows="2">
                                                            <!-- Row 1 -->
                                                            <Button Content="1" 
                                                                    Tag="1x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="3" 
                                                                    Tag="3x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="4" 
                                                                    Tag="2x2" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="4" 
                                                                    Tag="4x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="5" 
                                                                    Tag="5x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="6" 
                                                                    Tag="2x3" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="7" 
                                                                    Tag="7x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="8" 
                                                                    Tag="2x4" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="9" 
                                                                    Tag="3x3" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>

                                                            <!-- Row 2 -->
                                                            <Button Content="10" 
                                                                    Tag="2x5" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="10" 
                                                                    Tag="10x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="13" 
                                                                    Tag="13x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="16" 
                                                                    Tag="4x4" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="17" 
                                                                    Tag="17x1" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="25" 
                                                                    Tag="5x5" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="32" 
                                                                    Tag="4x8" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="36" 
                                                                    Tag="6x6" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                            <Button Content="64" 
                                                                    Tag="8x8" 
                                                                    Style="{StaticResource GridLayoutButtonStyle}" 
                                                                    Click="SelectLayout_Click"/>
                                                        </UniformGrid>
                                            </StackPanel>
                                </Grid>
                            </ScrollViewer>
                        </Border>
                    </Popup>
                </StackPanel>

                                <!-- Center - Media Control Icons -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                    <!-- Previous Button -->
                                    <Button x:Name="PreviousButton" 
                                            Content="⏮" 
                                            FontSize="14" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="28" Height="28"
                                            Margin="4,0"
                                            Cursor="Hand"
                                            Click="PreviousButton_Click"
                                            ToolTip="Previous"/>

                                    <!-- Play/Pause Button -->
                                    <Button x:Name="PlayPauseButton" 
                                            Content="▶" 
                                            FontSize="16" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="32" Height="32"
                                            Margin="4,0"
                                            Cursor="Hand"
                                            Click="PlayPauseButton_Click"
                                            ToolTip="Play/Pause"/>

                                    <!-- Next Button -->
                                    <Button x:Name="NextButton" 
                                            Content="⏭" 
                                            FontSize="14" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="28" Height="28"
                                            Margin="4,0"
                                            Cursor="Hand"
                                            Click="NextButton_Click"
                                            ToolTip="Next"/>
                                </StackPanel>
                
                                <!-- Right Side - Full Screen and Close Button -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <!-- Full Screen Button -->
                                    <Button x:Name="FullScreenButton" 
                                            Content="⛶" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Margin="0,0,4,0"
                                            Cursor="Hand"
                                            Click="FullScreenButton_Click"
                                            ToolTip="Full Screen"/>

                                    <Button Content="✕" 
                                            FontSize="12" 
                                            Foreground="{DynamicResource TextSecondaryColor}"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Width="24" Height="24"
                                            Cursor="Hand"
                                            Click="CloseButton_Click"
                                            ToolTip="Close"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
        </Grid>
    </DockPanel>
</Window>
