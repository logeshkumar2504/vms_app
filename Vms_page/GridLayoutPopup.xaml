<Window x:Class="Vms_page.GridLayoutPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Grid Layout" Height="400" Width="500"
        Background="{DynamicResource BackgroundColor}" 
        WindowStyle="SingleBorderWindow" 
        ResizeMode="NoResize"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner"
        Topmost="True">

    <Window.Resources>
        <!-- Layout Button Style -->
        <Style x:Key="LayoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource MenuHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PrimaryColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SidebarColor}" 
                Padding="16,12">
            <TextBlock Text="Select Grid Layout" 
                       Foreground="{DynamicResource TextPrimaryColor}" 
                       FontSize="16" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Layout Options -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Single View -->
                <Border Grid.Row="0" 
                        Background="{DynamicResource CardBackgroundColor}" 
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1" 
                        Margin="0,0,0,8"
                        CornerRadius="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Preview -->
                        <Border Grid.Column="0" 
                                Width="80" Height="60" 
                                Background="{DynamicResource SurfaceColor}" 
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                Margin="12,8">
                            <Grid>
                                <Border Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource WarningColor}"
                                        BorderThickness="1"
                                        Margin="2">
                                    <TextBlock Text="📹" 
                                               FontSize="20" 
                                               Foreground="{DynamicResource TextSecondaryColor}"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Description -->
                        <StackPanel Grid.Column="1" 
                                    VerticalAlignment="Center" 
                                    Margin="12,0,12,0">
                            <TextBlock Text="Single View (1x1)" 
                                       Foreground="{DynamicResource TextPrimaryColor}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="Display one camera feed in full screen" 
                                       Foreground="{DynamicResource TextSecondaryColor}" 
                                       FontSize="11" 
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <!-- Select Button -->
                        <Button Grid.Column="1" 
                                Content="Select" 
                                Style="{StaticResource LayoutButtonStyle}"
                                Width="60" Height="24"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,12,0"
                                Click="SelectLayout_Click"
                                Tag="1x1"/>
                    </Grid>
                </Border>

                <!-- 2x2 Grid -->
                <Border Grid.Row="1" 
                        Background="{DynamicResource CardBackgroundColor}" 
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1" 
                        Margin="0,0,0,8"
                        CornerRadius="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Preview -->
                        <Border Grid.Column="0" 
                                Width="80" Height="60" 
                                Background="{DynamicResource SurfaceColor}" 
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                Margin="12,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Border Grid.Row="0" Grid.Column="0" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="10" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="10" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="0" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource WarningColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="10" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="10" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Description -->
                        <StackPanel Grid.Column="1" 
                                    VerticalAlignment="Center" 
                                    Margin="12,0,12,0">
                            <TextBlock Text="Quad View (2x2)" 
                                       Foreground="{DynamicResource TextPrimaryColor}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="Display four camera feeds in a grid" 
                                       Foreground="{DynamicResource TextSecondaryColor}" 
                                       FontSize="11" 
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <!-- Select Button -->
                        <Button Grid.Column="1" 
                                Content="Select" 
                                Style="{StaticResource LayoutButtonStyle}"
                                Width="60" Height="24"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,12,0"
                                Click="SelectLayout_Click"
                                Tag="2x2"/>
                    </Grid>
                </Border>

                <!-- 3x3 Grid -->
                <Border Grid.Row="2" 
                        Background="{DynamicResource CardBackgroundColor}" 
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1" 
                        Margin="0,0,0,8"
                        CornerRadius="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Preview -->
                        <Border Grid.Column="0" 
                                Width="80" Height="60" 
                                Background="{DynamicResource SurfaceColor}" 
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                Margin="12,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 3x3 grid of small camera icons -->
                                <Border Grid.Row="0" Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource WarningColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.5" Margin="0.5">
                                    <TextBlock Text="📹" FontSize="6" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Description -->
                        <StackPanel Grid.Column="1" 
                                    VerticalAlignment="Center" 
                                    Margin="12,0,12,0">
                            <TextBlock Text="Nine View (3x3)" 
                                       Foreground="{DynamicResource TextPrimaryColor}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="Display nine camera feeds in a grid" 
                                       Foreground="{DynamicResource TextSecondaryColor}" 
                                       FontSize="11" 
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <!-- Select Button -->
                        <Button Grid.Column="1" 
                                Content="Select" 
                                Style="{StaticResource LayoutButtonStyle}"
                                Width="60" Height="24"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,12,0"
                                Click="SelectLayout_Click"
                                Tag="3x3"/>
                    </Grid>
                </Border>

                <!-- 4x4 Grid -->
                <Border Grid.Row="3" 
                        Background="{DynamicResource CardBackgroundColor}" 
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1" 
                        Margin="0,0,0,8"
                        CornerRadius="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Preview -->
                        <Border Grid.Column="0" 
                                Width="80" Height="60" 
                                Background="{DynamicResource SurfaceColor}" 
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                Margin="12,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 4x4 grid of very small camera icons -->
                                <Border Grid.Row="0" Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="3" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="3" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="2" Grid.Column="3" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="0" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="1" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="2" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource WarningColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="3" Grid.Column="3" Background="{DynamicResource SurfaceColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0.3" Margin="0.3">
                                    <TextBlock Text="📹" FontSize="4" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Description -->
                        <StackPanel Grid.Column="1" 
                                    VerticalAlignment="Center" 
                                    Margin="12,0,12,0">
                            <TextBlock Text="Sixteen View (4x4)" 
                                       Foreground="{DynamicResource TextPrimaryColor}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="Display sixteen camera feeds in a grid" 
                                       Foreground="{DynamicResource TextSecondaryColor}" 
                                       FontSize="11" 
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <!-- Select Button -->
                        <Button Grid.Column="1" 
                                Content="Select" 
                                Style="{StaticResource LayoutButtonStyle}"
                                Width="60" Height="24"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,12,0"
                                Click="SelectLayout_Click"
                                Tag="4x4"/>
                    </Grid>
                </Border>

                <!-- Custom Layout -->
                <Border Grid.Row="4" 
                        Background="{DynamicResource CardBackgroundColor}" 
                        BorderBrush="{DynamicResource BorderColor}"
                        BorderThickness="1" 
                        Margin="0,0,0,8"
                        CornerRadius="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Preview -->
                        <Border Grid.Column="0" 
                                Width="80" Height="60" 
                                Background="{DynamicResource SurfaceColor}" 
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="1"
                                Margin="12,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Custom layout preview with different cell sizes -->
                                <Border Grid.Row="0" Grid.Column="0" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource WarningColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="8" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="0" Grid.Column="1" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="8" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="0" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="8" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Grid.Row="1" Grid.Column="1" 
                                        Background="{DynamicResource SurfaceColor}" 
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        Margin="1">
                                    <TextBlock Text="📹" FontSize="8" Foreground="{DynamicResource TextSecondaryColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Description -->
                        <StackPanel Grid.Column="1" 
                                    VerticalAlignment="Center" 
                                    Margin="12,0,12,0">
                            <TextBlock Text="Custom Layout" 
                                       Foreground="{DynamicResource TextPrimaryColor}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="Create your own custom grid layout" 
                                       Foreground="{DynamicResource TextSecondaryColor}" 
                                       FontSize="11" 
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <!-- Select Button -->
                        <Button Grid.Column="1" 
                                Content="Custom" 
                                Style="{StaticResource LayoutButtonStyle}"
                                Width="60" Height="24"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,12,0"
                                Click="CustomLayout_Click"
                                Tag="custom"/>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <Border Grid.Row="2" 
                Background="{DynamicResource SidebarColor}" 
                Padding="16,8">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="Cancel" 
                        Style="{StaticResource LayoutButtonStyle}"
                        Width="80" Height="28"
                        Margin="0,0,8,0"
                        Click="Cancel_Click"/>
                <Button Content="Apply" 
                        Style="{StaticResource LayoutButtonStyle}"
                        Width="80" Height="28"
                        Click="Apply_Click"
                        x:Name="ApplyButton"
                        IsEnabled="False"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
